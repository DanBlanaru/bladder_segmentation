---
version: 1

kind: experiment

build:
  image: pytorch/pytorch:1.11.0-cuda11.3-cudnn8-runtime
  build_steps:
  - python --version
  - pip install -r requirements.txt
environment:
  resources:
    gpu:
      requests: 1
      limits: 1
run:
  cmd: python -u scripts/resize_amos.py
